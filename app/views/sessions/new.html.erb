<%= form_for @user, url: sessions_create_url do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>

  <%= f.label :password %>
  <%= f.password_field :password %><br>

  <%= f.submit 'Sign In' %>
<% end %>
